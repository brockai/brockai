name: Deploy to DigitalOcean

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Clone to DigitalOcean Droplet
      
        uses: appleboy/ssh-action@master
        with:
          host: ${{secrets.DROPLET_IP}}
          username: root
          key: ${{secrets.SSH_PRIVATE_KEY}}
          script: |
            rm -rf brockai
            echo 'Deployment to digital ocean finished'
      
      # - name: Build Services
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.DROPLET_IP }}
      #     username: ${{ secrets.DROPLET_USERNAME }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
          
      #     script: |
      #       cd brockai
      #       docker stop opensearch-node1 || true
      #       docker stop opensearch-node2 || true
      #       docker stop opensearch-dashboards || true
      #       docker compose down
      #       docker compose up opensearch-node1
      #       docker compose up opensearch-node2
      #       # docker compose up opensearch-dashboards